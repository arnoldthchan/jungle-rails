<% if review = Review.find_by(id: review_id) %>
  <% user = User.find_by(id: review.user_id) %>
  <% full_name = "#{user.first_name} #{user.last_name}" %>

  <% if user.id == current_user.id%>
    DELETE BUTTON HERE
  <% end %>

  <h4><%= "\u{2B50}" * review.rating %></h4>
  <em>By <%= full_name %> </em>
  <p/>
  <p><%= review.description %></p>
  <br>
<% end %>
